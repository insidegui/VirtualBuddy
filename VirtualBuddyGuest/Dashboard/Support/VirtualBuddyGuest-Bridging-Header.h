/// VirtualBuddyGuest previously used a separate launch at login helper, but since VirtualBuddy 2.2 beta 3, it registers itself as a login item.
/// This identifier and the legacy helper target are kept so that VirtualBuddyGuest can unregister it.
#define kLegacyGuestAppLaunchAtLoginHelperBundleID LEGACY_GUEST_LAUNCH_AT_LOGIN_HELPER_BUNDLE_ID
