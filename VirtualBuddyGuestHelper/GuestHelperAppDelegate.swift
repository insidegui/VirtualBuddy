import Cocoa

/// This launch at login helper is no longer used as of VirtualBuddy 2.2 beta 3, but is kept so that VirtualBuddyGuest can unregister it if needed.
@main
final class GuestHelperAppDelegate: NSObject, NSApplicationDelegate { }
